/**
 * View type id for the full-tab editor.
 *
 * Lives in its own module so the session and the view can both import it
 * without importing each other.
 */
export const VIEW_TYPE_DIAGRAM = "markdown-toolkit-diagram";
